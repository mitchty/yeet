syntax = "proto3";

package loglevel;

import "google/protobuf/empty.proto";

enum Level {
  TRACE = 0;
  DEBUG = 1;
  INFO = 2;
  WARN = 3;
  ERROR = 4;
  OFF = 5;
}

message Request {
  Level level = 1;
}

service LogLevel {
  rpc SetLevel(Request) returns (google.protobuf.Empty);
}
